{
  "name": "ryukanchi-human-device-trust-layer",
  "version": "0.1.0",
  "private": true,
  "description": "SomaGuard simulation-only TypeScript foundation for human-device trust layer research.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5",
    "vitest": "^1.4.0"
  }
}

